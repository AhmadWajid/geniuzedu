/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,ts,jsx,tsx}",
    // Add other content paths as needed
  ],
  darkMode: 'class', // Enable dark mode with class strategy
  theme: {
    extend: {
      // Any theme extensions you have
    },
  },
  plugins: [],
}
